[package]
name = "rox-python"
version = "0.1.0"
edition = "2021"
description = "Python bindings for Rhythm Open Exchange"

[lib]
name = "rox"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
rhythm-open-exchange = { path = "../.." }
