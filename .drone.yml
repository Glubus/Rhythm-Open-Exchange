kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: arm64
steps:
  - name: test
    image: rust:1.83
    commands:
      - cargo test

  - name: clippy
    image: rust:1.83
    commands:
      - rustup component add clippy
      - cargo clippy -- -D warnings

  - name: fmt
    image: rust:1.83
    commands:
      - rustup component add rustfmt
      - cargo fmt -- --check
